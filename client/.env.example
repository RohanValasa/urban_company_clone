# Copy to client/.env.local and fill in.
#
# Google Maps key for the location picker. Enable "Places API (New)" and
# "Geocoding API" on it, and restrict it to your site's domain.
# Leave it empty to use the free OpenStreetMap (Photon) search instead.
VITE_GOOGLE_MAPS_API_KEY=
